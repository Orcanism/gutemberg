"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const shorten_1 = __importDefault(require("./shorten"));
const ref = { latitude: 59.329394, longitude: 18.068712 };
const code = '9FFW84J9+XG';
it('returns null for non-string input', () => {
    expect(shorten_1.default(undefined, ref)).toEqual(null);
    expect(shorten_1.default(42, ref)).toEqual(null);
    expect(shorten_1.default({}, ref)).toEqual(null);
});
it('returns null on invalid code', () => {
    expect(shorten_1.default('invalid', ref)).toEqual(null);
});
it('returns null on invalid reference', () => {
    expect(shorten_1.default(code, {})).toEqual(null);
});
it('returns shortest code if reference is same as code', () => {
    expect(shorten_1.default(code, { latitude: 59.332438, longitude: 18.118813 })).toEqual('J9+XG');
});
it('returns the shortened code on a local reference', () => {
    expect(shorten_1.default(code, ref)).toEqual('84J9+XG');
});
it('returns the shortened code on a national reference', () => {
    expect(shorten_1.default(code, { latitude: 62.248996, longitude: 15.32562 })).toEqual('FW84J9+XG');
});
it('returns the full code on a global reference', () => {
    expect(shorten_1.default(code, { latitude: 59.329394, longitude: -162.068712 })).toEqual(code);
});
//# sourceMappingURL=shorten.spec.js.map