const Discord = require('discord.js');

const config = require('../config.json');

module.exports = {
    name: 'end',
    description: 'Permet de finir la partie et rends le salon accessible aux autres joueurs',

    async run(client, msg, args, author) {
    }
}