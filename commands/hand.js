const Discord = require('discord.js');

const config = require('../config.json');

module.exports = {
    name: 'hand',
    description: 'Affiche la main du joueur',

    async run(client, msg, args, author) {
    }
}