const Discord = require('discord.js');

const config = require('../config.json');

module.exports = {
    name: 'init',
    description: 'Permet de privatiser un channel et de lancer une session de jeu',

    async run(client, msg, args, author) {
    }
}